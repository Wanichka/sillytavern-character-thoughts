# sillytavern-character-thoughts
A SillyTavern UI extension that surfaces characters' inner thoughts and mood from &lt;char_thoughts>/&lt;char_mood> blocks. Thoughts are stored per chat; avatars are organised into per-AU profiles with a coloured-initial fallback. Matches the current theme.
